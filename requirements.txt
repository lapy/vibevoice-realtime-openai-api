# VibeVoice OpenAI-Compatible TTS Server Dependencies
# Python 3.13 + PyTorch 2.11 (CUDA wheel index via UV_EXTRA_INDEX_URL)
#
# Default (matches Docker): PyTorch cu126 / CUDA 12.6 toolchain (incl. Volta / V100)
#   UV_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu126 uv pip install -r requirements.txt
# Optional: cu128 / CUDA 12.8 wheels (no sm_70 in PyTorch 2.11 — avoid for V100)
#   UV_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu128 uv pip install -r requirements.txt

torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0

# VibeVoice from GitHub (pinned to specific commit)
git+https://github.com/microsoft/VibeVoice.git@a507d67f8e78d9ff6b2ec0b79f1f43ad89477187

# HuggingFace with fast downloads
huggingface_hub[hf_xet]

# FastAPI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Audio processing
scipy
numpy
pydub
