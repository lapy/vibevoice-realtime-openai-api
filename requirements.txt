# VibeVoice OpenAI-Compatible TTS Server Dependencies
# Python 3.13 + PyTorch 2.11 (CUDA wheel index via UV_EXTRA_INDEX_URL)
#
# Default (matches Docker): PyTorch cu126 / CUDA 12.6 toolchain (broad NVIDIA compatibility)
#   UV_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu126 uv pip install -r requirements.txt
# Some PyTorch cu128 builds omit older GPU architectures; use cu126 if your GPU is unsupported.
# Optional cu128 (typically newer GPUs only):
#   UV_EXTRA_INDEX_URL=https://download.pytorch.org/whl/cu128 uv pip install -r requirements.txt

torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0

# VibeVoice from GitHub (pinned to microsoft/VibeVoice main HEAD for Transformers 4.57+ / streaming fixes)
git+https://github.com/microsoft/VibeVoice.git@e73d1e17c3754f046352014856a922f8208fb5d3

# HuggingFace with fast downloads
huggingface_hub[hf_xet]

# FastAPI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Audio processing
scipy
numpy
pydub
